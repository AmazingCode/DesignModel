using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ConsoleApplication1
{
    /// <summary>
    /// 什么是开放封闭原则：例如类不能修改，但可以扩展这个类。也就是说对于更改是封闭的，对于扩展是开放的
    /// 设计类时尽量将类写的足够好，以后就不用再修改这个类了，如果有新需求，只要重新写一个类就行了。
    /// 目的是：面对需求，对程序的改动是增加新代码，而不是修改现有的代码
    /// </summary>
    class 开放封闭原则
    {

    }
}
